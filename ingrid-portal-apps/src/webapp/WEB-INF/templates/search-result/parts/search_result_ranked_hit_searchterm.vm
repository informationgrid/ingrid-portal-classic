#set($searchTerm = $UTIL_SEARCH.getDetailValueList( $rs.getHitDetail(), "t04_search.searchterm" ))
#set($isOpendata = $searchTerm.contains("opendata") || $searchTerm.contains("opendataident"))
#set($isInspire = $searchTerm.contains("inspire") || $searchTerm.contains("inspireidentifiziert"))
#set($accessConstraint = $UTIL_SEARCH.getDetailValue( $rs.getHitDetail(), "t011_obj_serv.has_access_constraint" ))
#set($isHVD = $UTIL_SEARCH.getDetailValue( $rs.getHitDetail(), "is_hvd" ) == "true")
#set($folder = $UTIL_SEARCH.getDetailValue( $rs.getHitDetail(), "object_node.tree_path.name", 1 ))
#renderSearchTerm($isOpendata $isInspire $accessConstraint $isHVD $folder)